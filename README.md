# testupsun

## Hello world script

Run the script with any Python 3 interpreter:

```
python hello.py
```

Expected output:

```
test new world
```

The script takes no arguments and always exits with status 0.

### Test

If pytest is installed:

```
pytest tests/test_hello.py
```
