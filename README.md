# testupsun

A minimal "Hello, World!" Python 3 program.

## Usage

Run the program directly:

```sh
python hello.py
```

It prints `Hello, World!` to standard output and exits with status 0. The
script uses only the Python standard library, so no setup or dependencies are
required.
