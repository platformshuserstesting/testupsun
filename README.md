# testupsun

A minimal Python test program (SCRUM-10).

## Run the program

```bash
python main.py
```

Expected output:

```
Hello from testupsun!
2 + 3 = 5
```

## Run the tests

Install the dev dependencies and run the suite:

```bash
pip install -r requirements-dev.txt
pytest -q
```
