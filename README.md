# testupsun

A minimal "Hello, World!" Python 3 program.

## Usage

Run the program directly:

```bash
python hello.py
```

It prints `Hello, World!` to standard output and exits with status code 0.
The `main()` function is guarded by an `if __name__ == "__main__"` entry point,
so importing the module does not produce any output.
